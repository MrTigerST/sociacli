{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay window — only needs to invoke its own `set_visible` command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-set-visible",
    "opener:allow-open-url"
  ]
}
